android.useAndroidX=true
# Jetifier is only needed for legacy (pre-AndroidX) libraries. Keep disabled unless required.
android.enableJetifier=false

# Windows stability: reduce file locking issues during clean/rebuild.
org.gradle.vfs.watch=false
kotlin.compiler.execution.strategy=in-process

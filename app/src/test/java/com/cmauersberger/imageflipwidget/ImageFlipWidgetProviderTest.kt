package com.cmauersberger.imageflipwidget

import org.junit.Test
import org.junit.Assert.*

class ImageFlipWidgetProviderTest {
    
    @Test
    fun testWidgetProviderExists() {
        // Simple test to verify the widget provider class exists
        val provider = ImageFlipWidgetProvider()
        assertNotNull(provider)
    }
}
